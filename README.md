# Project Simulation

## Опис проєкту

Проєкт є бакалаврською роботою на тему **«Моделювання процесу зближення та стикування космічних апаратів»**.

Програма реалізує чисельне моделювання процесу зближення космічних апаратів, обчислює параметри відносного руху та дозволяє аналізувати умови стикування.

## Структура проєкту

* `cosmos2025.cpp` — основний файл програми
* `library.cpp`, `library.h` — логіка математичного моделювання
* `tests/` — модульні тести
* `CMakeLists.txt` — конфігурація збірки
* `docs/` — документація з розгортання, оновлення та резервного копіювання
* `scripts/` — скрипти автоматизації

## Архітектура проєкту

Проєкт є консольним застосунком і не використовує вебсервер, application server, СУБД або кешування.

Схема роботи:

Користувач
↓
Консольний застосунок
↓
Модуль обробки вхідних параметрів
↓
Модуль математичного моделювання
↓
Обчислення траєкторії та перевірка умови стикування
↓
Вивід результатів у консоль або файл

## Вимоги до середовища розробки

Для запуску та розробки проєкту потрібні:

* Git
* CMake 3.20 або новіше
* C++ компілятор з підтримкою C++17
* IDE або редактор коду:

    * CLion, або
    * Visual Studio / VS Code, або
    * будь-який інший редактор з підтримкою C++

## Підготовка середовища

### Windows

1. Встановити Git
2. Встановити CMake
3. Встановити компілятор C++:

    * MinGW, або
    * Visual Studio Build Tools, або
    * MSVC через Visual Studio

### Linux

1. Встановити Git
2. Встановити CMake
3. Встановити g++

Наприклад для Ubuntu:

```
sudo apt update
sudo apt install git cmake g++
```

## Клонування репозиторію

```
git clone https://github.com/OhChilled/Project.git
cd untitled2
```

## Збірка проєкту

```
mkdir build
cd build
cmake ..
cmake --build .
```

## Запуск проєкту

Після успішної збірки виконуваний файл буде знаходитися в каталозі `build`.

### Linux

```
./untitled2
```

### Windows

```
.\untitled2.exe
```

## Запуск у режимі розробки

1. Внести зміни у вихідний код
2. Перезібрати проєкт:

```
cmake --build .
```

3. Повторно запустити виконуваний файл

## Робота з тестами

Якщо тести налаштовані в CMake, їх можна запустити так:

```
ctest
```

## Базові команди

### Перша збірка

```
mkdir build
cd build
cmake ..
cmake --build .
```

### Повторна збірка

```
cd build
cmake --build .
```

### Запуск тестів

```
cd build
ctest
```
## Linting

Для статичного аналізу коду використовується clang-tidy.
Для форматування коду використовується clang-format.

Приклад запуску:

cmake -S . -B build -DCMAKE_EXPORT_COMPILE_COMMANDS=ON
cmake --build build
clang-tidy cosmos2025.cpp library.cpp -- -I.

## Автор

Ткаченко Данііл
